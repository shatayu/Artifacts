// Copy this to Config-Local.xcconfig and fill in API_KEY
// xcconfig has a quirk: literal "//" starts a comment, so use $() to break it up in URLs
API_BASE_URL = http:/$()/localhost:3000
API_KEY = REPLACE_WITH_LOCAL_API_KEY
